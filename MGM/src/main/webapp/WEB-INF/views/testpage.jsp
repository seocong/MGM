<%@ page language="java" contentType="text/html; charset=UTF-8"
	pageEncoding="UTF-8"%>
<%@ taglib uri="http://java.sun.com/jsp/jstl/core" prefix="c"%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>testtest</title>
</head>
<body>
	<%-- <table>
		<c:forEach begin="1" end="${num}" varStatus="sta">
		<c:set var="doneLoop" value="false" />
		<tr>
			<td>
			<c:forEach items="${testList}" var="list">
					<c:if test="${list eq sta.count && not doneLoop}">
							${list}
							<c:set var="doneLoop" value="true" />
					</c:if>
					<c:if test="${list ne sta.count && not doneLoop}">
						gg
						<c:set var="doneLoop" value="true" />
					</c:if>
			</c:forEach>
			</td>
		</tr>
		</c:forEach>
	</table> --%>
</body>
</html>